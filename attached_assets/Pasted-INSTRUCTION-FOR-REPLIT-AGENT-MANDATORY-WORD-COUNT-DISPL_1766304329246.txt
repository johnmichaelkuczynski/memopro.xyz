INSTRUCTION FOR REPLIT AGENT: MANDATORY WORD COUNT DISPLAY

THIS IS A HARD REQUIREMENT. DO NOT SKIP THIS.
The output display MUST show a word count. Users need to verify output length at a glance. This has been requested multiple times and not implemented. Implement it now.
IMPLEMENTATION:

Add word count display to the Validation Result component:

jsx// In the Validation Result (RECONSTRUCTION) component:

<div className="validation-result">
    <div className="result-header">
        <h3>✨ Validation Result (RECONSTRUCTION)</h3>
        
        {/* WORD COUNT - MANDATORY - DO NOT REMOVE */}
        <div className="word-count-display" style={{
            backgroundColor: '#f0f9ff',
            border: '1px solid #0ea5e9',
            borderRadius: '8px',
            padding: '8px 16px',
            marginBottom: '16px',
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center'
        }}>
            <span style={{ fontWeight: 'bold' }}>
                📊 Output Word Count: {wordCount.toLocaleString()} words
            </span>
            {targetWords && (
                <span style={{ 
                    color: wordCount >= targetWords ? '#16a34a' : '#dc2626',
                    fontWeight: 'bold'
                }}>
                    {wordCount >= targetWords ? '✓' : '⚠️'} Target: {targetWords.toLocaleString()}
                </span>
            )}
        </div>
        
        {/* Action buttons */}
        <div className="actions">
            <button>Download</button>
            <button>Send To →</button>
            <button>Copy</button>
            <button>Redo</button>
            <button>Clear</button>
        </div>
    </div>
    
    {/* Output content */}
    <div className="result-content">
        {outputText}
    </div>
</div>

Calculate word count whenever output changes:

javascript// Utility function - add to utils or helpers
function countWords(text) {
    if (!text) return 0;
    return text.trim().split(/\s+/).filter(word => word.length > 0).length;
}

// In component
const [wordCount, setWordCount] = useState(0);

useEffect(() => {
    if (outputText) {
        setWordCount(countWords(outputText));
    }
}, [outputText]);
```

3. **Display format examples:**
```
📊 Output Word Count: 5,847 words                    Target: 11,000 ⚠️
```
```
📊 Output Word Count: 11,234 words                   Target: 11,000 ✓
```
```
📊 Output Word Count: 15,892 words                   (no target specified)
```

4. **Position: The word count MUST be visible WITHOUT scrolling.** Place it:
   - Directly below the "Validation Result (RECONSTRUCTION)" header
   - Above the action buttons (Download, Send To, Copy, etc.)
   - OR in a sticky header that remains visible while scrolling through output

5. **Also show in download filename:**
```
Reconstruction_11234words_2024-12-21.docx
```

6. **Also show during processing (streaming view):**
```
═══════════════════════════════════════════════════
Processing: Chunk 7 of 72 
📊 Words so far: 4,847 / 15,000 target (32%)
═══════════════════════════════════════════════════
WHY THIS IS NON-NEGOTIABLE:

User requested output of specific length (e.g., "11,000 words minimum")
User cannot verify compliance without visible word count
User should not have to copy text into external tool to count words
This is basic UX for any document generation tool

VERIFY IMPLEMENTATION:
After implementing, the word count should be visible:

 Immediately when output appears
 Without scrolling
 With comparison to target (if user specified one)
 With clear pass/fail indicator (✓ or ⚠️)

DO THIS NOW.